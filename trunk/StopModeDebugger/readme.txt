This set of files provides Symbian OS debug primitives for a host Symbian OS-aware debugger.

The code set is not immediately buildable and runable, but requires suitable customisation for the 
appropriate host platform.